/*
Q- Virtual DOM
virtual dom is a programming concept where representation of a UI is kept in memory and synced with the “real” DOM by a library such as ReactDOM. This process is called reconciliation.

Q- SPA
 SPA (Single-page application) is a web app implementation that loads only a single web document

Q- Function component
 A functional component is just a plain JavaScript function that accepts props as an argument and returns a React element(JSX).

Q- Class component
 A class component requires you to extend from React and to use class we have to use methods again and again.

Q- State
 The state is a built-in React object that is used to contain data or information about the component and we can change it and re-render it.

 Q- JSX
 JSX allows us to write HTML elements in JavaScript and place them in the DOM without any createElement() methods.

 Q-Package.json
 package. json is a metadata file that lists all the dependencies required for the project to run.

 Q- React and React Native
 React utilizes HTML, CSS, and JavaScript to create interactive user interfaces, whereas React Native utilizes APIs and native UI components to build mobile applications.
 React is a library and react Native is a react framework.

 Q-Routing Package
 react-router-dom is used to install routing.


*/